import { InjectionToken } from '@angular/core';

/**
 * SharedState class - for observer & observable
 */
export class SharedState {
    constructor () {}
}

export const SHARED_STATE = new InjectionToken("shared_state");